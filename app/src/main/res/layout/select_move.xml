<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="200dp"
    android:layout_height="200dp">

    <TextView
        android:id="@+id/moveName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="45dp"
        android:layout_marginTop="36dp"
        android:text="TextView"
        android:fontFamily="@font/opensansbold"
        android:textSize="15sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/movePP"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="TextView"
        android:fontFamily="@font/opensansbold"
        android:textSize="15sp"
        app:layout_constraintStart_toStartOf="@+id/moveName"
        app:layout_constraintTop_toBottomOf="@+id/moveName" />

    <Button
        android:id="@+id/select_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="@string/select"
        android:backgroundTint="@color/blue"
        app:layout_constraintStart_toStartOf="@+id/movePP"
        app:layout_constraintTop_toBottomOf="@+id/movePP" />
</androidx.constraintlayout.widget.ConstraintLayout>